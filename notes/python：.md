python：

四种复杂类型：列表（数组）、集合、tuple（元组）、字典（相当于java中的键值对）

可变的数据类型：列表、字典、集合

有序的数据类型：列表、元组、字符串



随机获得一个值：

1.整数：random.randint(数的范围，包含关系)

2.小数：random.unifrom(数的范围，包含关系)

3.获取0-1之间的随机数：random.random()



基本运算：

1.2个星号表示此方；

2.字符串是可以运算的；

3.python中是没有swicth判断语句的

4.append（）是给元素添加括号里面的内容；

5.def 函数名（）：  	（必须得打冒号）；

6.和java不同的是if在java中是要打括号的，但是在pyth中打冒号的；在pyhton中循环不需要和java中必须要打大括号来包裹语句，python中只需要连在一起不出现空行就行；

7.range（n）：表示生成一个从0到n-1的一个列表；

8.切片：a = "my name is XXX" 	a = a[1:5]	表示左闭右开，每个数表示的下标

a = a[1:5:1]  第三个位置表示每次输出隔着几个数字，前面两个如果为负数就是倒着来。

9.替换某一段：a = a.replace(“xxx”,"pig") 		

split：分割split()里面可以是符号，根据这个符号来分割数据，然后方法的底层是返回一个列表；

string = "-".join(arr)	: 引号里面是指需要用什么符号来进行组合字符串，join是组合字符串的函数和java中的toString方法类似的。

10.排序：a.sort() 和 a = sorted(a) 他们的区别是第一个直接改变原来的元素，后者则是返回一个新的列表；



